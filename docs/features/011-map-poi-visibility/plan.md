# Plan

Trạng thái: Implementing (đã được yêu cầu triển khai).

1. Tạo builder URL giữ nguyên mặc định và thêm tham số style POI khi tắt.
2. Nối state/callback checkbox vào effect tile, cập nhật dependency và cleanup retry timer khi thay lớp.
3. Chạy lint, TypeScript, build; ghi riêng kết quả cấu trúc URL và ảnh provider.
4. Không đánh dấu Verified nếu thiếu ảnh xác nhận ẩn POI; báo giới hạn môi trường.

