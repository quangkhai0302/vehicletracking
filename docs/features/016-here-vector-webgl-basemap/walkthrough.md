# Walkthrough

Restart backend và frontend. Chọn Đường bộ hoặc Ban đêm trong “Lớp bản đồ”, mở DevTools và xác nhận canvas MapLibre GL hiện diện; pan/zoom vẫn dùng Leaflet. Chọn Vệ tinh để xác nhận fallback raster. Bật traffic và chọn xe để xác nhận layer vận hành còn phía trên nền.
