# Spec — Project reset

Frontend chỉ giữ entry point, `App`, CSS nền và cấu hình build. Backend chỉ giữ
application class, `application.yaml`, context-load test, Maven wrapper và `pom.xml`.
Không có components, service, controller, entity, repository, provider hoặc API key.
