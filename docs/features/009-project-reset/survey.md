# Survey — Project reset

Trước reset, frontend còn Leaflet/CARTO map; repository còn cấu hình HERE/Google,
file `.env`, tài liệu feature `001–008`, workflow cũ và build artifacts. Backend
runtime chỉ còn application class, cấu hình tên ứng dụng và context-load test.
